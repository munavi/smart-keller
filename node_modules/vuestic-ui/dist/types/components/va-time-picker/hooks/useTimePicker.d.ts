import { Ref } from 'vue';
interface TimePickerProps {
    ampm: boolean;
    hidePeriodSwitch: boolean;
    periodUpdatesModelValue: boolean;
    view: 'hours' | 'minutes' | 'seconds';
    modelValue?: Date;
    hoursFilter?: (h: number) => boolean;
    minutesFilter?: (h: number) => boolean;
    secondsFilter?: (h: number) => boolean;
    readonly?: boolean;
}
declare type ModelValueRef = Ref<Date | null | undefined>;
export declare const useTimePicker: (props: TimePickerProps, modelValue: ModelValueRef) => {
    columns: import("vue").ComputedRef<({
        items: number[];
        activeItem: import("vue").WritableComputedRef<number>;
    } | {
        items: string[];
        activeItem: import("vue").WritableComputedRef<number>;
    })[]>;
    isPM: Ref<boolean>;
};
export {};

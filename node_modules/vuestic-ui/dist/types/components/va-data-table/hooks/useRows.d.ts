import { Ref } from 'vue';
import { TableColumn, ITableItem, TableRow } from '../types';
interface useRowsProps {
    items: ITableItem[];
    [prop: string]: unknown;
}
export default function useRows(columns: Ref<TableColumn[]>, props: useRowsProps): {
    rowsComputed: import("vue").ComputedRef<TableRow[]>;
};
export {};

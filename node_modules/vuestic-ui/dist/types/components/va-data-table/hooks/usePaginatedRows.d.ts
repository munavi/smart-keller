import { Ref } from 'vue';
import { TableRow } from '../types';
interface useSelectableProps {
    perPage: number | undefined;
    currentPage: number | undefined;
    [prop: string]: unknown;
}
export default function usePaginatedRows(sortedRows: Ref<TableRow[]>, props: useSelectableProps): {
    paginatedRows: import("vue").ComputedRef<TableRow[]>;
};
export {};

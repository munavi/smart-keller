import { Ref } from 'vue';
import { TableRow } from '../types';
interface useSelectableProps {
    currentPage: number | undefined;
    animated: boolean;
}
export default function usePaginatedRows(props: useSelectableProps, rows: Ref<TableRow[]>): Ref<string>;
export {};
